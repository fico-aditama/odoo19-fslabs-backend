# ── Minimal — sama pola dengan bridge. Sisanya ditarik dari Odoo. ────────────
ODOO_URL=http://localhost:8069
AGENT_SECRET=ganti_random_panjang     # samakan dengan AI Config → Agent Secret di Odoo
AGENT_PORT=8800

# Lokal di host agent (bukan rahasia, path filesystem Chroma)
CHROMA_DIR=./chroma_db

# CATATAN: OPENAI_API_KEY, model, embed model, Langfuse keys, Chroma collection
# TIDAK di sini — semua diatur di Odoo (AI Config) dan ditarik otomatis saat start.
